import Joi from "joi"

const createClubValidation = {
    body: Joi.object({

    })
}

const editClubValidation = {
    body: Joi.object({
        
    })
}


export {
    createClubValidation,
    editClubValidation
}